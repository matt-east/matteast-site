{
  "name": "matteast-site",
  "version": "1.0.0",
  "description": "Matt East personal website",
  "private": true,
  "scripts": {
    "build": "npx esbuild analytics.js --bundle --format=iife --outfile=analytics.bundle.js --platform=browser",
    "dev": "npx http-server -p 3000"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0"
  }
}
